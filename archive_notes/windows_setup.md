# Windows setup notes

Historical setup note extracted from `Tools/getANewStart.ipynb`.

Install the Files Community file manager:

```powershell
winget install FilesCommunity.Files
```

The old notebook also contained an unfinished reminder to install JupyterLab. It was not a complete environment specification and is not preserved as an executable setup script.
